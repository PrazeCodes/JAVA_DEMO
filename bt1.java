public class bt1 {

        // public static void printPermutations(String str, String perm) {
        //     // Base case: if the permutation is the same length as the original string, print it
        //     if (perm.length() == str.length()) {
        //         System.out.println(perm);
        //         return;
        //     }
    
        //     // Loop through each character in the original string
        //     for (int i = 0; i < str.length(); i++) {
        //         char ch = str.charAt(i);
        //         // Check if the character is already in the current permutation
        //         if (perm.indexOf(ch) == -1) { // Only proceed if ch is not already in perm
        //             // Create a new string without the current character
        //             String newString = str.substring(0, i) + str.substring(i + 1, str.length());
        //             // Recursive call to get all possible combinations
        //             printPermutations(newString, perm + ch);
        //         }
        //     }
        // }
    
        public static void main(String args[]) {
            // String str = "ABCD";
            // printPermutations(str, "");

            System.out.println("Pranjal");
        }
    } 

